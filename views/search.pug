extends layout

block content
    #search.login-register-wrapper.wrapper.col-sm-12
        div.col-sm-6.col-sm-offset-3.col-md-4.col-md-offset-4
            wrapper.col-sm-12
                form(method="POST", action="/search")
                    input.form-control.my-form(type='text', name='name', placeholder='SEARCH')
                    button#magnif.span.glyphicon.glyphicon-search.btn(type='submit')