extends layout
block content
    #products.container
        each product in products
            slideanim
                .col-sm-4
                    .thumbnail
                        a(href="/product/"+product._id)
                            .image
                                img.img-responsive(src=product.imagePath)
                            p
                                strong #{product.name}