extends layout
block content
    #product.container
        slideanim
            .col-sm-8.col-sm-offset-2
                .thumbnail
                    .image
                        img.img-responsive(src=product.imagePath)
                    p
                        strong #{product.name}
                    p #{product.description}
                    br
                    br
                    p #{product.rating}
                    .rating
                        span &star;
                        span &star;
                        span &star;
                        span &star;
                        span &star;